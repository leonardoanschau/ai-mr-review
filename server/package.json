{
  "name": "gitlab-mcp-server",
  "version": "1.0.0",
  "description": "GitLab MCP Server - Model Context Protocol server for GitLab integration",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "tsc && node dist/server.js",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "mcp",
    "gitlab",
    "model-context-protocol"
  ],
  "author": "AnschauTI",
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
